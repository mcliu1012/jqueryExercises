<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
    xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:jee="http://www.springframework.org/schema/jee"
    xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/jee
  		http://www.springframework.org/schema/jee/spring-jee-3.1.xsd">

    <!-- Root Context: defines shared resources visible to all other web
        components -->

	<!-- properties -->
	<context:annotation-config />
    <context:property-placeholder location="classpath:jdbc.properties" />
    <context:property-placeholder location="classpath:mcliu.properties" />

	<!-- 方式一：    如果写这行代码，可搭配   在server.xml中配置数据库信息   使用。
		  因为现在把数据库信息放到了pom.xml中，所以暂时注掉该行代码 -->
<!-- 	<jee:jndi-lookup id="dataSourceMcliu" jndi-name="java:comp/env/jdbc/dataSourceMcliu" /> -->

	<!-- 方式二 -->
	<beans:bean id="dataSourceMcliu"
        class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <beans:property name="driverClassName" value="${jdbc.mcliu.driverClassName}" />
        <beans:property name="url" value="${jdbc.mcliu.url}" />
        <beans:property name="username" value="${jdbc.mcliu.username}" />
        <beans:property name="password" value="${jdbc.mcliu.password}" />
    </beans:bean>
    <beans:bean class="org.springframework.jdbc.core.JdbcTemplate">
        <beans:property name="dataSource" ref="dataSourceMcliu" />
    </beans:bean>
</beans:beans>
